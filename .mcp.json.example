{
  "mcpServers": {
    "open-brain": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--directory", "open-brain-cli", "open-brain-cli"],
      "env": {
        "BRAIN_API_URL": "${BRAIN_API_URL}",
        "BRAIN_API_KEY": "${BRAIN_API_KEY}",
        "BRAIN_SERVICE_KEY": "${BRAIN_SERVICE_KEY}"
      }
    }
  }
}
