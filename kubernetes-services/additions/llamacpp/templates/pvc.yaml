apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llamacpp-models
  namespace: llamacpp
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "" # static binding — no dynamic provisioner
  volumeName: llamacpp-models-nfs
  resources:
    requests:
      storage: 900Gi
