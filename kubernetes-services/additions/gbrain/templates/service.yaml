apiVersion: v1
kind: Service
metadata:
  name: gbrain
  labels:
    app: gbrain
spec:
  type: ClusterIP
  ports:
    - port: 3131
      targetPort: 3131
      protocol: TCP
      name: http
  selector:
    app: gbrain
