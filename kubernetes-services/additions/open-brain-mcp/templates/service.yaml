apiVersion: v1
kind: Service
metadata:
  name: open-brain-mcp
  labels:
    app: open-brain-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: open-brain-mcp
