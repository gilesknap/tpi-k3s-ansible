# Placeholder — replace with actual SealedSecret output from kubeseal.
# See docs/how-to/open-brain.md for instructions.
#
# Required keys:
#   DATABASE_URL: postgresql://supabase_admin:PASSWORD@supabase-supabase-db.supabase.svc.cluster.local:5432/postgres
#   MCP_JWT_SECRET: random 64-char hex string
#   GITHUB_CLIENT_ID: from GitHub OAuth App
#   GITHUB_CLIENT_SECRET: from GitHub OAuth App
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: open-brain-mcp-secret
  namespace: open-brain-mcp
spec:
  encryptedData:
    DATABASE_URL: REPLACE_WITH_KUBESEAL_OUTPUT
    MCP_JWT_SECRET: REPLACE_WITH_KUBESEAL_OUTPUT
    GITHUB_CLIENT_ID: REPLACE_WITH_KUBESEAL_OUTPUT
    GITHUB_CLIENT_SECRET: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: open-brain-mcp-secret
      namespace: open-brain-mcp
