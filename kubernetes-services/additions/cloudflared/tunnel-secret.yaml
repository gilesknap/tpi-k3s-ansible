# PLACEHOLDER — this file must be replaced with a real SealedSecret before ArgoCD can sync cloudflared.
#
# To generate: retrieve the TUNNEL_TOKEN for tunnel `gk2` from the Cloudflare Zero Trust dashboard,
# then run the following on a machine with kubectl and kubeseal configured against the live cluster:
#
#   kubectl create secret generic cloudflared-credentials \
#     --namespace cloudflared \
#     --from-literal=TUNNEL_TOKEN=<token> \
#     --dry-run=client -o yaml | \
#     kubeseal --controller-namespace kube-system -o yaml > \
#     kubernetes-services/additions/cloudflared/tunnel-secret.yaml
#
# Then commit and push the resulting SealedSecret. ArgoCD will decrypt and apply it automatically.
