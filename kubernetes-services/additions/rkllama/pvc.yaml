apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rkllama-models
  namespace: rkllama
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "" # static binding — no dynamic provisioner
  volumeName: rkllama-models-nfs
  resources:
    requests:
      storage: 900Gi
