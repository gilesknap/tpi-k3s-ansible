[project]
name = "open-brain-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "asyncpg",
    "httpx",
    "pyjwt[crypto]",
    "starlette",
    "uvicorn[standard]",
]
